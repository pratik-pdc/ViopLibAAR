plugins {
    id 'com.android.library'
}

android {
    namespace 'com.pdc.voiplibaar'
    compileSdk 33

    defaultConfig {
        minSdk 21
        targetSdk 33
    }

    configurations.maybeCreate("default")
    artifacts.add("default", file('voiplib-1.0.0.aar'))
}
