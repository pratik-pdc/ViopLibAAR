plugins {
    id 'com.android.library'
}

android {
    namespace 'com.pdc.voiplib'
    compileSdk 33

    defaultConfig {
        minSdk 21
        targetSdk 33
    }

    configurations.maybeCreate("default")
    artifacts.add("default", file('app-debug.aar'))
}
